export type IEmployee = {
  id: string;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  companyId: string;
  jobTitle: string;
  jobArea: string;
  jobType: string;
};

export const employees: IEmployee[] = [];
